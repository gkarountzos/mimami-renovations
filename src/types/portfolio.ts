export type TPortfolioContent = {
  title: string;
  subtitle: string;
  areas: string[];
  categories: string[];
};

export type TPortfolioImage = {
  id: number;
  title: string;
  location: string;
  category: string;
  image: string;
};
